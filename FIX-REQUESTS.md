# Covenia 修复清单

审批状态：APPROVED
批准人：Zack（在 Codex 对话中明确批准）
批准日期：2026-09-10
对应冻结文件：`PRODUCT-FREEZE.md` P0-10 至 P0-12
对应实现基线：`feat/c-qianniu-plugin` / `50a5a99f`

## 已批准修复

| ID | 缺陷证据 | 期望行为 | 验收方法 | 优先级 | 是否影响主 Demo |
|---|---|---|---|---|---|
| B1/B2 | Mock 以当前机器时间重建时限，示例时间线残留旧状态 | 从服务端装载的有效承诺读取截止与下次检查；Mock 使用可注入服务端时钟，P0-4 时间线保持 09:40 评估、09:42 批准、10:10 揽收、11:35 未揽收 | 任意机器日期下重复运行；断言承诺时限、下次检查和回执一致 | 高 | 是 |
| B3/A34 | `challenge_overrides` 未受门控 | 仅 `challenge_mode: true` 时应用覆盖；否则完全忽略并回显 false | A21、A22、A34 自动化测试 | 高 | 是 |
| B4/B5 | 第一屏未展示完整状态、承诺原文/执行人/时限 | 展示六种 `case_status`，并在第一屏展示承诺原文、状态、截止时间和执行人 | 界面检查与状态映射测试 | 高 | 是 |
| B6/B7 | ALLOW 显示防线条，主管跟踪区缺失 | 防线条仅用于 INTERVENE/HUMAN_REVIEW；展示催办及主管升级候选 | 界面检查 | 中 | 是 |
| B8 | 成本条没有正式数据来源 | `runtime_metrics` 进入 TypeScript 契约、JSON Schema 与响应，再由右下成本条读取 | 契约断言、界面检查 | 中 | 是 |
| B9 | 批准接口未校验审批人 | 缺 `approver_id` 返回不可重试的 `VALIDATION_ERROR` | A11 自动化测试 | 高 | 是 |
| B10/B11 | 规则未读取动作、禁止项或不良反应 | 按 P0-10 计算 P0/H1/E1/E2/E0，并写入被压制规则 | A13、A14、A15、A20/A21、A29/A30 自动化测试 | 高 | 是 |
| B12 | TypeScript 与 Schema 的卫生风险字段不一致 | 统一为 `hygiene_risk_signal` | TypeScript 编译与 JSON Schema 校验 | 高 | 否 |
| D1–D6 | 已批准的联动修正 | 仅按冻结范围修正交接、示例及演示一致性 | 回归检查 | 中 | 是 |
| D7 | 示例保留旧表述/展示 ID | `02` 使用 P0-8 请求形状，残留展示 ID 统一 `DEMO_001`；兼容字段保留 | JSON 检查 | 中 | 否 |

## 验证门

按 `frontend/package-lock.json` 安装锁定依赖，运行 `npm test -- --run` 与 `npm run build`，将真实用例数、通过数及构建结果写入 `IMPLEMENTATION-STATUS.md`。该项是修复交付前验证门，不计入功能缺陷编号。

## 明确不修

| 缺陷 ID | 不修理由 | 风险接受人 |
|---|---|---|
| D8/P1 打磨项 | 不在本次批准的有限修复范围内 | Zack |
| 图片文件名与 `80525870445254.PNM` | 两者是团队演示图片名与赛事消息来源 ID，不构成冲突 | Zack |

## 禁止范围扩展

不新增页面、模块、售后场景或第五个接口；不删除任何 Schema 合法枚举值或 evaluate 兼容字段；不改写 `CLAUDE-REVIEW-1.md`；不扩展 S00001 以外的业务场景。
