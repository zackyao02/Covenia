# Covenia 数据与模拟边界声明

## 官方赛事 Mock 数据

官方文件原名 `赛题 1：数据共情者-业务数据.xlsx`，仓库内改名为 `tianchi-track1-mock-data.xlsx` 置于 `data/`，按相对路径导入。文件明确声明全部内容均为 AI 生成的虚构 Mock 数据。工作簿共 8 个表，其中「数据说明」不是运行输入；项目使用其 7 个业务表完成数据导入、跨表关联、场景审计和系统验证，不将其中比例描述为真实消费者或欧莱雅生产业务结论。

进入模型前的脱敏范围：手机号、收货地址、支付宝账号、就医与不良反应描述先做掩码，掩码后的文本才进入 Prompt。每次调用记录 `pii_masked_count` 并写入治理记录。原始字段仍保留在本地导入结果中用于事实核对，不随模型输入外发。

## 团队扩充数据

`S00001` 原始记录只包含一条图片消息和图片路径引用，工作簿不含图片文件。主演示增加的两张证据图片、第二次进线、评估时间和物流事件均属于团队构建的压力测试数据，必须在案例元数据和界面中标记。

正确表述：

> 基于赛事 `S00001` 会话、订单和换货工单构建的压力测试案例。

不得表述为“欧莱雅真实消费者复联案例”或“赛事数据已经证明重复索证发生”。

## 统计边界

赛事文件可以支持：138 个会话、998 条消息、113 笔订单、29 条图片消息、80 条五类工单以及 28 条非完结工单等结构性统计。

重复索证、承诺违约率、消费者情绪发生率和业务 ROI 必须来自独立人工标注、真实调研或企业 Pilot，不能从人为扩充场景反推。

## 政策与执行边界

- 演示政策 `DEMO_POLICY_PACKAGE_DAMAGE_V1` 为模拟规则，不代表欧莱雅真实售后政策。
- 服务进度回执不是法律凭证。
- Covenia 不自动退款、赔偿、补发、质量定责或医疗诊断。
- 物流事件为 Mock Adapter 输入，不宣称接入生产 OMS、WMS、TMS 或千牛账号。

## 界面标识

工作台固定显示一次“赛事 Mock 数据＋团队压力测试扩充”。风险区分别标识“赛事数据统计”和“人工标注测试假设”。
