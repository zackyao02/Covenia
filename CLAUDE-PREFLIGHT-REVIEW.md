# Covenia Competition MVP v0.7｜Claude 开发前终审包

## 任务

你不是来扩写方案。你是消费者体验产品终审评委、一线客服运营负责人、AI 风险审查人和赛事技术评委。请把功能数量视为交付风险，通过明确取舍，把 Covenia 收敛为一线客服能使用、评委能迅速理解、现场可稳定演示的 Competition MVP。

本轮结论用于优化并生成 `PRODUCT-FREEZE.md` 草案。Claude 可在产品负责人明确要求后直接创建或更新该草案，但必须保留 `审批状态：DRAFT`，不能批准范围，不能修改产品基线文档、代码或 Schema。除非能直接提高 Hero 主链的可运行性、用户价值或 Demo 说服力，否则不得建议新增能力。

## 必须读取的证据

完整读取并交叉核对：

- `README.md`
- `PRODUCT_BLUEPRINT.md`
- `TEAM_AND_TIMELINE.md`
- `docs/00-product-p0.md`
- `docs/01-core-contracts.md`
- `docs/02-ai-extraction.md`
- `docs/03-firewall-rules.md`
- `docs/04-responsibility-loop.md`
- `docs/05-api-and-ui.md`
- `docs/06-evaluation-and-research.md`
- `docs/07-demo-and-delivery.md`
- `schemas/*.schema.json`
- `fixtures/*.json` 与 `fixtures/README.md`
- `MODEL_GOVERNANCE.md`
- `SIMULATION_DISCLOSURE.md`

每个判断必须指向具体证据。文件之间冲突时列出双方路径，不自行选择新产品方向。没有正式定义的字段、行为或结果必须标为“待冻结”或“待验证”。

## 固定事实与硬边界

- Covenia 是嵌入千牛客服工作台右侧的消费者体验责任副驾，不替代千牛。
- 单一 Hero 为 `S00001`：粉底液泵头损坏；消费者已提交图片；客服已确认包装破损、创建换货单，并承诺“48 小时内发出”。
- Hero 必须真实跑通：聊天、订单、工单和团队测试图片输入 → 结构化 `ExtractedJourney` → 承诺校验 → `INTERVENE / ALLOW / HUMAN_REVIEW` → 人工确认 → 责任与服务进度回执 → 物流事件驱动状态、催办候选和通知草稿变化。
- 主 Demo 不切换其他售后场景；范围变化、图片模糊和物流状态分支只用于 Challenge Mode。
- 不自动退款、赔偿、补发、医疗诊断或质量责任认定；高风险动作保留人工确认。
- 不宣称接入千牛或欧莱雅生产系统，不用模拟数据证明真实市场发生率。

## 必须裁决的问题

### 1. 一线客服可用性

- 当前设计是否错误假设客服懂美妆质量、证据范围、承诺权限和责任状态？
- 第一屏是否只回答“已知什么、不能做什么、下一步做什么”？
- 哪些提示、培训、人工确认或主管升级是 P0；哪些培训看板或复杂解释应删除？

### 2. 客服 KPI

审查以下权重能否形成可测量、可归因的辅助指标：响应速度 20%、客户满意度 20%、询单转化率 30%、品退率 10%、商品差评 10%、平台求助/投诉 10%。必须区分：

- 产品能直接记录和改善的过程指标；
- 只能通过对照实验观察的结果指标；
- 受商品、价格、物流等外部因素影响，不能由 Covenia 承诺的指标。

### 3. API 与 Schema

逐项检查并裁决是否进入 P0：

- 四个核心接口是否统一使用 `{ data, error, request_id }` 响应外壳；
- `POST /api/resolutions/approve` 的准确字段、人工修改结构、审批人、时间和幂等键；
- `POST /api/events/assignment` 或实际事件接口的能力、升级候选、状态变化和回包；
- HTTP 错误码、业务错误码、超时、模型失败、重复事件和幂等行为；
- 案例列表/详情、外发进度、聊天记录分别由哪个接口提供；
- “发送消费者回复”是后端真实动作、前端模拟展示还是仅生成草稿；
- TypeScript API 类型和四组 JSON 请求/响应样例是否必须在开发前冻结。

### 4. 红队与治理

覆盖错误抽取、范围错误导致误拦截、越权承诺、提示注入、敏感信息泄露、不可追溯决策、物流事件乱序/重复、人工修改未留痕、失败回滚和灾备结果冒充真实运行。

### 5. Demo 与交付

判断 4 分钟 Demo 是否能在前 20 秒说明消费者价值，并分别证明数据真实进入、AI 抽取真实、防线判断真实、责任真实运行、证据可核验。不得用固定动画冒充接口执行。

## 输出契约

输出必须使用以下标题，且只给一套结论：

1. `A. 总体裁决`：通过 / 有条件通过 / 不通过，以及最核心理由。
2. `B. 证据冲突与缺口`：文件路径、当前表述、冲突/缺失、必须由谁冻结。
3. `C. P0 阻塞项`：证据、影响、最小修改、验收条件；明确未解决前禁止启动的开发。
4. `D. P1/P2`：说明延后不会破坏 Hero 的理由。
5. `E. 一线客服与 KPI 裁决`：训练、辅助、升级与可测量指标。
6. `F. API 与最小架构裁决`：端点、类型、Schema、错误、幂等和前后端职责。
7. `G. 红队 Top 10`：攻击前置条件、步骤、影响、防护和验收用例。
8. `H. 单一 Hero Demo`：时间、画面、操作、证明点和失败兜底。
9. `I. 建议删除或降级`：功能、处理方式、成本、Demo 收益、理由。
10. `J. PRODUCT-FREEZE 输入摘要`：只提供候选内容，不生成或修改冻结文件。

每个 P0 必须具有可执行的验收条件；禁止使用“加强、优化、完善、注意”等无法验收的措辞。不要输出代码、不要修改产品基线文档或 Schema。先在交互终端完整展示报告，供产品负责人阅读；收到“生成产品冻结草案”指令后，才可按 `handoff-templates/PRODUCT-FREEZE.template.md` 创建或更新 `PRODUCT-FREEZE.md`，且审批状态必须保持 `DRAFT`。
