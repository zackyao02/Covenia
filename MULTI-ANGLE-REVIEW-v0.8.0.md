# Covenia v0.8.0 多角度评审与修订记录

评审日期：2026-09-08  
评审对象：产品文档、数据与模拟声明、领域 Schema、样例、Claude 开发前终审与独立红队结论。  
结论：**产品范围不扩张，文档工程包可进入实现阶段。** 实现前必须以 `PRODUCT-FREEZE.md`、`docs/05-api-and-ui.md` 和 `schemas/` 为准。

## 评审结果

| 维度 | 发现 | v0.8.0 修订 | 实现验收 |
|---|---|---|---|
| 产品与一线工作流 | 客服不必然熟悉质量、证据与售后权限；原材料缺少 KPI 边界 | 第一屏固定为“已知、不能、下一步”；匿名 KPI 输入限定为 n=1 设计参考 | 1366×768 首屏无需滚动即可完成动作判断 |
| 产品范围 | Hero 主链易被看板和新场景稀释 | 保持 S00001、四接口、三类结果与一个责任闭环 | 无独立培训、BI、售前转化或生产系统对接 |
| 接口契约 | 原先没有正式请求 Schema，且引用不存在的 `contracts.ts` | 新增 4 个请求 Schema 与统一响应外层 | 所有端点按 Schema 校验，并返回 `request_id` |
| 防线可信边界 | 客户端可伪造状态绕过 E1 | `evaluate` 只信任 case、动作与受限挑战变体；状态一律服务端计算 | A20–A22、A33 |
| 模型与提示注入 | 消费者文本可伪造承诺，图片文字可伪造证据结论 | 承诺仅取客服侧消息；图中文字不进入规则事实 | A25、A26 |
| 高风险与责任 | 越权承诺、责任倒流、假性结案缺少可验证门禁 | 明确激活约束、禁止动作映射、规则优先级与审计 | A28–A30 |
| 事件可靠性 | 送达可绕过揽收，重放与倒序未定义 | 建立事件状态机、幂等规则、错误码与验证向量 | A27、A32、V1–V5 |
| 隐私与治理 | 敏感字段“默认不入模”与“脱敏入模”表述不一致 | 定义字段级掩码、最小化占位和 `pii_masked_count` 记录 | A23、A24 |
| 演示诚实性 | 缓存可能被误认为实时模型结果 | 响应、画面和治理记录三处同时标识缓存 | A31；抗演员段禁止缓存 |
| 数据与研究 | KPI、赛事 Mock、团队扩充易被误作真实业务结论 | 数据来源分层，KPI 标记 n=1，售前转化不桥接 | 演示画面显示来源标识 |

## 新增工程资产

- `schemas/api-envelope.schema.json`
- `schemas/analyze-case-request.schema.json`
- `schemas/evaluate-action-request.schema.json`
- `schemas/approve-resolution-request.schema.json`
- `schemas/shipment-event-request.schema.json`
- `docs/08-idempotency-and-ordering.md`
- `reference/ANONYMIZED-CUSTOMER-SERVICE-KPI-NOTE.md`

## 尚待实现时验证的事项

1. 真实 Excel 导入应打印 138 / 998 / 113 / 29 / 80 五项计数，且长数字不丢失精度。
2. 模型、掩码器、状态构建器和规则引擎需在运行工程中按 A1–A33 完成自动化或可复现手测。
3. 5 张团队测试图片、30 个标注案例、模型版本与运行环境仍由开发与数据线提交；不能用文档代替运行证据。
4. 最终录屏必须在同一工程中完成主链和抗演员验证，缓存灾备只能作为明确标识的备用画面。

## 交接原则

Claude 的职责到产品评审、红队和独立验收为止。Codex 依据本包完成实现、测试、页面与最终打磨；所有新增范围、修复范围或产品取舍仍需产品负责人通过 Codex 明确批准。
